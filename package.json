{
  "name": "lerna-typescript-cra-uilib-starter",
  "version": "1.0.0",
  "description": "A monorepo to create game companion apps and utilities",
  "main": "index.js",
  "repository": "https://github.com/carloscasalar/game-companions.git",
  "author": "@carloscasalar",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "3.20.2"
  },
  "scripts": {
    "story": "lerna run story --stream",
    "prestart": "lerna run libbuild --stream",
    "start": "lerna run start --stream",
    "test": "lerna run test --stream",
    "test-ci": "lerna run test-ci --stream"
  }
}
